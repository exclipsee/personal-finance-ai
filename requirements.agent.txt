# Optional dependencies for the local AI agent helpers
openai>=0.27.0
python-dotenv>=1.0.0
# Optional: install the Kaggle client if you plan to use Kaggle fetching
# kaggle>=1.5.12
python-dateutil>=2.8.2
# Optional: Plaid SDK (install only when enabling Plaid integration)
# plaid-python>=9.0.0
scikit-learn>=1.4.0
numpy>=1.26.0
joblib>=1.3.2
# Optional: faiss-cpu for faster local vector search
# faiss-cpu>=1.7.3
# Optional: sentence-transformers for local dense embeddings
# sentence-transformers>=2.2.2
cryptography>=41.0.0
celery>=5.3.0
redis>=4.6.0
